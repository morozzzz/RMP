import React from 'react';
// import PosterTile from './PosterTile';

const GridList = ({ movies = [] }) => (
    <div className="grid-list">
        {
            // movies.map(movieData => (
            //     <PosterTile data={movieData} key={movieData.id} />
            // ))
        }
    </div>
);

export default GridList;
